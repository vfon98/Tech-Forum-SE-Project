import { primaryColor, secondaryColor, textColor } from './main'
import { green } from '@material-ui/core/colors'

const ReportPopupStyles = {
  reportBg: {
    backgroundColor: primaryColor,
    color: textColor,
  },
  textareaLabel: {
    marginTop: '0.2em',
    marginBottom: '0.5em'
  }
}

export default ReportPopupStyles;