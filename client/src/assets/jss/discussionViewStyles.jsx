import { container } from "./main";

const discussionViewStyles = {
  container: {
    ...container
  },
  background: {
    background: "#fff",
    padding: '5rem 0'
  }

}

export default discussionViewStyles;